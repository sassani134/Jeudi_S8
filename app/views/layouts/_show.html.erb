 <div>
   <p id="notice"><%= notice %></p>
</div>

<div id="contenu">
	<p>
		<h2>Object: <%= @email.title %></h2>

	</p>
	<p>
	  <strong>Body :</strong><br>
	  <%= @email.body %>
	  <br>
	  <%= link_to 'Delete', @email, method: :delete, data: { confirm: 'Are you sure?' } %>
	</p>
</div>

